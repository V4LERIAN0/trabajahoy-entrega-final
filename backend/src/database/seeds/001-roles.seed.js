// Seed: Insert default roles (admin, recruiter, candidate, moderator)
// Already handled in migration 002-CreateRolesTable.js
export async function seedRoles(dataSource) {
  console.log('Roles seed already handled in migration. Skipping.');
}
